services:
  sstv-iptv:
    build: .
    container_name: sstv-iptv
    ports:
      - "3025:3025"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./cache:/app/cache
    restart: unless-stopped
